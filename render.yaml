services:
  - type: web
    name: sonix-lite
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: "./render-build.sh"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true
    aptPackages:
      - ffmpeg